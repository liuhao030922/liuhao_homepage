pages:
# NOTE: `name` field must match the `navbar_title` field in the page's front matter to ensure the correct page is highlighted in the navbar.
#       For example, the `navbar_title` field in `showcase.html` is "Others" and the `name` field here should also be "Others"
- name: Home
  url: /
- name: Others
  url: /showcase
